---
title: "Get ERC-1155 Token Transfers by Address and Contract"
sidebarTitle: "ERC-1155 Transfers"
description: "Retrieve ERC-1155 multi-token transfer events for a wallet, filtered by contract and block range. Supports pagination using the page and offset parameters."
---

Use this endpoint to retrieve the full ERC-1155 multi-token transfer history for a given address. ERC-1155 supports both fungible and non-fungible tokens within a single contract. You can optionally filter by a specific contract address or block range, and paginate through results using `page` and `offset`.

<Warning>
  **Effective July 1, 2026**, the maximum records returned per request will be reduced from 10,000 to 1,000 for Free tier API users. See the full list of [affected endpoints](https://docs.etherscan.io/changelog#upcoming-change-reduced-maximum-records-per-request-on-the-free-api-tier).
</Warning>

## Query Parameters

<ParamField query="apikey" type="string" default="YourApiKeyToken">
  Your Etherscan API key.
</ParamField>

<ParamField query="chainid" type="string" default="1">
  Chain ID to query, e.g. `1` for Ethereum, `8453` for Base from our [supported chains](/supported-chains).
</ParamField>

<ParamField query="module" type="string" default="account">
  Set to `account` for this endpoint.
</ParamField>

<ParamField query="action" type="string" default="token1155tx">
  Set to `token1155tx` for this endpoint.
</ParamField>

<ParamField query="contractaddress" type="string" default="0x76be3b62873462d2142405439777e971754e8e77">
  The ERC-1155 token contract address to filter transfers by, e.g. `0x495f947276749ce646f68ac8c248420045cb7b5e` for [Opensea Shared Storefront](https://etherscan.io/token/0x495f947276749ce646f68ac8c248420045cb7b5e).
</ParamField>

<ParamField query="address" type="string" default="0x83f564d180b58ad9a02a449105568189ee7de8cb">
  The address to query, like `0xfefefefefefefefefefefefefefefefefefefefe`.
</ParamField>

<ParamField query="page" type="integer" default="1">
  Page number for pagination.
</ParamField>

<ParamField query="offset" type="integer" default="100">
  Number of records per page. Use the `page` parameter for subsequent records.
</ParamField>

<ParamField query="startblock" type="integer" default="0">
  Starting block number to search from.
</ParamField>

<ParamField query="endblock" type="integer" default="999999999">
  Ending block number to search to.
</ParamField>

<ParamField query="sort" type="string" default="asc">
  Sort order: `desc` for the latest transactions first, or `asc` for the oldest transactions first.
</ParamField>

## Example Request

```bash
curl "https://api.etherscan.io/v2/api\
?chainid=1\
&module=account\
&action=token1155tx\
&contractaddress=0x76be3b62873462d2142405439777e971754e8e77\
&address=0x83f564d180b58ad9a02a449105568189ee7de8cb\
&startblock=0\
&endblock=999999999\
&page=1\
&offset=100\
&sort=asc\
&apikey=YourApiKeyToken"
```

## Response

The `result` array contains one object per ERC-1155 transfer event.

<ResponseField name="status" type="string">
  `"1"` on success, `"0"` on failure.
</ResponseField>

<ResponseField name="message" type="string">
  `"OK"` on success, or an error description on failure.
</ResponseField>

<ResponseField name="result" type="array">
  Array of ERC-1155 transfer objects.

  <ResponseField name="blockNumber" type="string">
    Block number in which the transfer was included.
  </ResponseField>

  <ResponseField name="timeStamp" type="string">
    Unix timestamp of the block.
  </ResponseField>

  <ResponseField name="hash" type="string">
    Transaction hash.
  </ResponseField>

  <ResponseField name="nonce" type="string">
    Sender's transaction count at the time of this transaction.
  </ResponseField>

  <ResponseField name="blockHash" type="string">
    Hash of the block containing this transaction.
  </ResponseField>

  <ResponseField name="transactionIndex" type="string">
    Position of the transaction within the block.
  </ResponseField>

  <ResponseField name="gas" type="string">
    Gas limit set for the transaction.
  </ResponseField>

  <ResponseField name="gasPrice" type="string">
    Gas price in wei.
  </ResponseField>

  <ResponseField name="gasUsed" type="string">
    Actual gas consumed by the transaction.
  </ResponseField>

  <ResponseField name="cumulativeGasUsed" type="string">
    Total gas used in the block up to and including this transaction.
  </ResponseField>

  <ResponseField name="input" type="string">
    Deprecated. Always returns `"deprecated"`.
  </ResponseField>

  <ResponseField name="methodId" type="string">
    First four bytes of the call data, identifying the function invoked.
  </ResponseField>

  <ResponseField name="functionName" type="string">
    Human-readable name of the function called, if available.
  </ResponseField>

  <ResponseField name="contractAddress" type="string">
    ERC-1155 token contract address.
  </ResponseField>

  <ResponseField name="from" type="string">
    Sender address of the token transfer.
  </ResponseField>

  <ResponseField name="to" type="string">
    Recipient address of the token transfer.
  </ResponseField>

  <ResponseField name="tokenID" type="string">
    The token ID within the ERC-1155 contract.
  </ResponseField>

  <ResponseField name="tokenValue" type="string">
    Quantity of tokens transferred for this token ID.
  </ResponseField>

  <ResponseField name="tokenName" type="string">
    Name of the ERC-1155 collection, e.g. `"parallel"`.
  </ResponseField>

  <ResponseField name="tokenSymbol" type="string">
    Token ticker symbol, e.g. `"LL"`.
  </ResponseField>

  <ResponseField name="confirmations" type="string">
    Number of block confirmations since this transaction was mined.
  </ResponseField>
</ResponseField>

<ResponseExample>
```json Response
{
  "status": "1",
  "message": "OK",
  "result": [
    {
      "blockNumber": "13472395",
      "timeStamp": "1634973285",
      "hash": "0x643b15f3ffaad5d38e33e5872b4ebaa7a643eda8b50ffd5331f682934ee65d4d",
      "nonce": "41",
      "blockHash": "0xa5da536dfbe8125eb146114e2ee0d0bdef2b20483aacbf30fed6b60f092059e6",
      "transactionIndex": "100",
      "gas": "140000",
      "gasPrice": "52898577246",
      "gasUsed": "105030",
      "cumulativeGasUsed": "11739203",
      "input": "deprecated",
      "methodId": "0x3e6b214b",
      "functionName": "",
      "contractAddress": "0x76be3b62873462d2142405439777e971754e8e77",
      "from": "0x1e63326a84d2fa207bdfa856da9278a93deba418",
      "to": "0x83f564d180b58ad9a02a449105568189ee7de8cb",
      "tokenID": "10371",
      "tokenValue": "1",
      "tokenName": "parallel",
      "tokenSymbol": "LL",
      "confirmations": "9995266"
    }
  ]
}
```
</ResponseExample>
